<%= form_with(model: order_name) do |form| %>
  <% if order_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_name.errors.count, "error") %> prohibited this order_name from being saved:</h2>

      <ul>
        <% order_name.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, class: 'form-control' %>
  </div>


  <%= form.hidden_field :user_id, value: current_user.id %>
  

  <div class="field">
    <%= form.label :system_name_id %>
    <%= form.collection_select(:system_name_id, SystemName.all, :id, :content) %>
  </div>


  <div class="actions">
    <%= form.submit '登録', class: 'btn btn-success btn-sm btn3d' %>
  </div>
<% end %>
